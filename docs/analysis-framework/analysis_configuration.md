# Analysis Configuration

The configuration system stores runtime values supplied by users.

Parameter definitions are stored separately in the Parameter Schema.

This separation allows:

- validation
- UI generation
- serialization
- reusable configurations

Configuration behaves as an immutable mapping.